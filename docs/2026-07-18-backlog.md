# Backlog — atualizado 2026-07-18

Lista viva do que está ABERTO. Histórico completo da rodada anterior (20 itens,
14 fechados) em `docs/2026-07-17-gaps-backlog.md`. Itens de segurança seguem
NEUTROS aqui — detalhes em documento externo ao repo.

## Aberto — precisa de DECISÃO antes de codar

| # | Item | Decisão pendente | Custo após decidir |
|---|---|---|---|
| D1 | Recarga visível nos jogadores remotos — o `playerUpdate` não carrega estado de recarga (verificado servidor+cliente em 2026-07-18) | adicionar campo ao protocolo de rede? (aumenta payload por tick) | P/M — a animação reusa o padrão `stepHolster` |
| D2 | `ak-47_reddot.glb` parado no repo, fora do arsenal | mapear numa arma nova/skin ou remover o asset | P |

## Aberto — gatilhado (não atacar antes do gatilho)

| # | Item | Gatilho |
|---|---|---|
| G1 | Tier 1 server-side (validação de geometria/estado) + bots conhecendo paredes/LOS — caminho técnico mapeado (PRNG independente + builder puro de paredes) | telemetria de produção acusar evento impossível |
| G2 | `todRate` específico do modo solo (DAY_LEN 480 mantido) | playtest do solo acusar ritmo lento |
| G3 | Rampas 18–20° do caminhão exigem embalo (contrato `driveable` atual) | design pedir partida parada acima de ~16° |

## Aberto — polish opcional (baixa prioridade, sem gatilho)

| # | Item | Custo |
|---|---|---|
| P1 | Trilhas de pneu só no veículo DIRIGIDO — estender a carros remotos usando poses já recebidas da rede (sem autoridade nova) | P |
| P2 | Deslize residual dos pés do esqueleto (passada alternada existe; IK de pé plantado ficou fora de escopo por decisão) | M |
| P3 | e2e Python roda 1× sem triagem de flake (o runner JS já tria sozinho) — replicar o "re-roda isolado" no wrapper do e2e | P |

## Regras vivas (não são tasks)

- `br-rank.json` (runtime do servidor) e `assets/models/boss-castle.v1.glb` (do dev): NUNCA staged/commitados.
- `?segs=440` é ferramenta de MEDIÇÃO de terreno — adoção de 2,5 m foi medida e RECUSADA em 2026-07-18 (heap +135 MB, benefício físico zero); não reabrir sem dado novo.
- Upstream do Will morto — PR/merge interno no nosso repo; prod puxa do fork.

## Estado de referência

- Produção: `game.renatodreis.com.br` no commit `5d7f28d` (2026-07-18), suíte 445 pass.
- Validação visual: `scripts/capture-skeletons.js`, `scripts/gif-from-frames.sh`, padrão captura+leitura de imagem.
- Triagem de flake: automática no `scripts/run-tests.js`.

# Gaps 8–13 (terreno/clima/veículos) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fechar os itens 8–13 do `docs/2026-07-17-gaps-backlog.md` (hill-start do caminhão, chuva por gota + cobertura do campo e da nave, trilhas na grama, A/B de resolução 2,5 m, DAY_LEN) mapeando as lacunas ANTES de cada correção.

**Architecture:** Todas as mudanças são de APRESENTAÇÃO ou física local do cliente — nada toca dano, autoridade de rede ou o stream do RNG seedado da geração do mundo. Cobertura de chuva continua centralizada em `js/cover.js` (retângulos O(1), zero raycast); tração do caminhão reusa o caminho AWD que já existe em `js/car.js:338-339`; trilhas reusam o pipeline instanciado de `js/grass.js` (atributo novo por lâmina, mesmo padrão de `aPhase`/`aTint`).

**Tech Stack:** three.js r184, cannon-es 0.20, node:test, puppeteer-core (harness `test/helpers/harness.js`), ESLint.

## Global Constraints

- **RNG seedado é contrato** (CLAUDE.md): nenhuma chamada nova a `rand()`/`Math.random()` dentro da geração do mundo (`js/terrain.js`, `js/grass.js`, `js/structures.js`). Tudo aqui é runtime ou metadado puro.
- **Destruição da cidade é mecânica intencional**: o telhado climático da cidade já some/volta via `Structures.city.onStateChange` (`game.js:1768-1778`, source `'city'`). As fontes novas do campo usam source `'campo'` e NÃO podem ser afetadas por esse evento.
- **Cover é só apresentação/áudio** (`js/cover.js:4-5`): nada de física, dano ou autoridade. Provider da nave usa pose/fase já validadas — nunca flag solta do cliente.
- **Sem aumentar `force`/`grip` de veículo nenhum**; limites do servidor 55/90/120 km/h intocados. Total de força do caminhão continua `2 × 3600` — só muda a distribuição, e apenas em hill-start.
- **Testes**: browser usa portas FIXAS. Em uso: 3164–3200, 3213, 3217–3224, 3227, 3230, 3232, 3234. Porta nova deste plano: **3236** (hill-start). Rodar arquivos à mão SEMPRE com `--test-concurrency=1`. Flake ≠ bug: re-rodar isolado 2–3× antes de declarar regressão.
- **Não matar a porta 3000** (servidor vivo do dev). `npm run lint` limpo (`no-unused-vars` é erro).
- **Commits locais OK; PR pro upstream do Will é à parte** (item 20 do backlog, fora deste plano).

---

## Fase 0 — Mapa de lacunas (verificado no código em 2026-07-17)

Fatos confirmados por leitura direta — é a base de cada task:

| # | Lacuna | Evidência |
|---|---|---|
| 8 | Caminhão RWD: `force: 3600` aplicado inteiro em cada roda traseira | `js/car.js:297`, `js/car.js:341-342` |
| 8 | Caminho AWD JÁ existe (sport `awd: true` divide `force*0.5` nas 4) | `js/car.js:303`, `js/car.js:338-339` |
| 8 | Grip do caminhão já foi a 2.0 por hill-start ≥14° (círculo de fricção) | comentário em `js/car.js:297` |
| 8 | Contrato duplo já implícito: corredor de teste ≤12° ("hill-start parado"), `driveable` mundial ≤20° | `test/car-terrain-traversal.test.js:40-43`, `js/terrain.js:137` |
| 9 | Chuva some INTEIRA sob cobertura: `showRain` exige `camExposure > 0.02` e opacidade × `camExposure` | `js/env.js:140-142` |
| 9 | Gota só é classificada no RESPAWN (`p.hidden`), nunca reclassificada | `js/env.js:148-152` |
| 9 | Provider da nave cobre QUALQUER coordenada durante SHIP | `br-game.js:693-694` |
| 9 | A nave tem `worldToLocal(pose, world, out)` pronto e `DIMS` (outerRadius 18, ceilingY 3.25) | `ship-protocol.js:79`, `ship-protocol.js:20-35` |
| 10 | Cover do campo vem SÓ de `platforms` (lajes) — forro da cabana e laje da torre são geometria mesclada, sem metadado | `game.js:1761-1766`, `js/structures.js:41-80` |
| 10 | Tipos de site do campo: `torre`, `cabana`, `ruína`, `forte`, `base`, `cidade` (NÃO existem "tendas") | `js/structures.js` (`sites.push`) |
| 10 | `js/structures.js:502`: "Nada aqui pode consumir o RNG seedado" — metadado puro é o padrão da casa | `js/structures.js:502` |
| 11 | Grama: 13×13 chunks × ~1005 lâminas (`GRASS_TOTAL` 170k), atributos instanciados `aPhase`/`aTint`, `DynamicDrawUsage`, RNG local por chunk, reciclagem via `fillChunk` | `js/grass.js:18-22,239-252,180-237` |
| 12 | Grade canônica 5 m: `buildHeightGrid(CFG.WORLD_SIZE)` usa o default `worldSize/5`; visual e Heightfield usam `CFG.TERRAIN_SEGS` 220 | `game.js:81,194-204,229`, `js/terrain.js:77`, `js/config.js:12-13` |
| 12 | `heightAt` (da grade) alimenta a GERAÇÃO das structures (`game.js:338`) → mudar a resolução MUDA o layout do mundo pro mesmo seed | `game.js:338`, `js/terrain.js:69-86` |
| 13 | `DAY_LEN = 480` canônico e documentado; decisão: manter | `js/climate.js:5-10` |

Interações com eventos que cada task precisa respeitar:

- **Destruição da cidade** (`city-destruction-protocol`): remove/restaura só o source `'city'` do Cover. Task 2 prova por teste que `'campo'` sobrevive.
- **Fase SHIP**: provider dinâmico tem precedência no `coverAt` (`js/cover.js:42-45`). Task 4 troca "cobre tudo" por teste geométrico real, mantendo o contrato "verdade = fase + pose validada".
- **Testes existentes que estas mudanças quebram de propósito (RED)**: `test/weather-cover.test.js:75` (`dentro === 0`) conta QUALQUER gota num raio de 30 m — com chuva visível pela janela isso passa a ser o comportamento DESEJADO; o teste muda para "zero gotas em posição coberta" + "gotas visíveis lá fora".

---

### Task 1: Probe físico de hill-start (node, sem browser) — mede itens 8 e 12

**Files:**
- Create: `scripts/probe-hillstart.mjs`
- Create: `docs/plans/2026-07-17-gaps-8-13-mapa.md` (resultados medidos)

**Interfaces:**
- Consumes: parâmetros físicos reais de `js/car.js:40-98` (replicados como constantes; o probe NÃO importa `js/car.js` — GLTFLoader/THREE pesam e não afetam a física).
- Produces: tabela JSON `dist6s[veículo][grau][célula][modo]` que calibra os alvos do teste da Task 6 e alimenta a decisão da Task 8.

- [ ] **Step 1: Escrever o probe**

```js
#!/usr/bin/env node
/* Probe determinístico de hill-start: rampas sintéticas (Heightfield) ×
   inclinação × resolução da célula (5 m vs 2,5 m) × tração (RWD vs 4x4
   de arranque). Replica os parâmetros REAIS de js/car.js:40-98 — se
   mudar lá, mude aqui. Saída: JSON no stdout. */
import * as CANNON from 'cannon-es';

const SUSP_REST = 0.55;                       // js/car.js:15
const suspStatic = k => SUSP_REST - 9.82 / (4 * k);
const VEHS = {
  buggy: { mass: 280, half: [1.8, 0.38, 0.85], comDrop: 0.65, suspStiff: 24, force: 1650, grip: 1.9,
    wheels: [[1.147, -0.993, 0.612], [1.147, -0.993, -0.612], [-0.814, -0.993, 0.612], [-0.814, -0.993, -0.612]],
    wheelR: [0.207, 0.207, 0.207, 0.207] },
  truck: { mass: 680, half: [2.7, 0.55, 1.05], comDrop: 0.3, suspStiff: 20, force: 3600, grip: 2.0,
    wheels: [[1.673, -0.387, 0.916], [1.673, -0.387, -0.916], [-0.812, -0.343, 0.916], [-0.812, -0.343, -0.916]],
    wheelR: [0.437, 0.437, 0.481, 0.481] },
};

function rampWorld(deg, cell) {
  const world = new CANNON.World({ gravity: new CANNON.Vec3(0, -9.82, 0) });
  const t = Math.tan(deg * Math.PI / 180), n = Math.round(160 / cell) + 1;
  const data = [];
  for (let i = 0; i < n; i++) {
    const row = [];
    for (let j = 0; j < n; j++) row.push(i * cell * t);
    data.push(row);
  }
  const body = new CANNON.Body({ mass: 0 });
  body.addShape(new CANNON.Heightfield(data, { elementSize: cell }));
  body.position.set(-80, 0, 80);
  body.quaternion.setFromAxisAngle(new CANNON.Vec3(1, 0, 0), -Math.PI / 2);
  world.addBody(body);
  return { world, hAt: x => (x + 80) * t };
}

function makeVehicle(world, cfg, hAt) {
  let spawnY = -Infinity;                      // nasce apoiado (js/car.js:44-50)
  for (let i = 0; i < 4; i++)
    spawnY = Math.max(spawnY, hAt(cfg.wheels[i][0]) + cfg.wheelR[i] - cfg.wheels[i][1] + 0.03);
  const chassis = new CANNON.Body({ mass: cfg.mass, position: new CANNON.Vec3(0, spawnY - cfg.comDrop, 0) });
  chassis.addShape(new CANNON.Box(new CANNON.Vec3(...cfg.half)), new CANNON.Vec3(0, cfg.comDrop, 0));
  chassis.angularDamping = 0.42; chassis.linearDamping = 0.02;
  chassis.allowSleep = false;                  // probe controla o arranque
  const veh = new CANNON.RaycastVehicle({ chassisBody: chassis, indexRightAxis: 2, indexForwardAxis: 0, indexUpAxis: 1 });
  for (let i = 0; i < 4; i++) {
    const [wx, wy, wz] = cfg.wheels[i];
    veh.addWheel({
      radius: cfg.wheelR[i], directionLocal: new CANNON.Vec3(0, -1, 0),
      suspensionStiffness: cfg.suspStiff, suspensionRestLength: SUSP_REST,
      frictionSlip: cfg.grip, dampingRelaxation: 3.5, dampingCompression: 4.4,
      maxSuspensionForce: 100000, rollInfluence: 0.01, axleLocal: new CANNON.Vec3(0, 0, 1),
      chassisConnectionPointLocal: new CANNON.Vec3(wx, wy + cfg.comDrop + suspStatic(cfg.suspStiff), wz),
      maxSuspensionTravel: 0.3, customSlidingRotationalSpeed: -30, useCustomSlidingRotationalSpeed: true,
    });
  }
  veh.addToWorld(world);
  return { chassis, veh };
}

function run(vName, deg, cell, mode) {
  const { world, hAt } = rampWorld(deg, cell);
  const { chassis, veh } = makeVehicle(world, VEHS[vName], hAt);
  for (let s = 0; s < 120; s++) {              // 2 s assentando com freio de mão
    for (let i = 0; i < 4; i++) { veh.setBrake(12, i); veh.applyEngineForce(0, i); }
    world.step(1 / 60);
  }
  const x0 = chassis.position.x;
  const force = VEHS[vName].force;
  for (let s = 0; s < 360; s++) {              // 6 s de acelerador
    for (let i = 0; i < 4; i++) veh.setBrake(0, i);
    const kmh = chassis.velocity.length() * 3.6;
    let awdK = 0;
    if (mode === 'hill4x4' && kmh < 15) awdK = Math.min(1, (15 - kmh) / 5);
    const perFront = force * 0.5 * awdK, perRear = force * (1 - 0.5 * awdK);
    veh.applyEngineForce(perFront, 0); veh.applyEngineForce(perFront, 1);
    veh.applyEngineForce(perRear, 2); veh.applyEngineForce(perRear, 3);
    world.step(1 / 60);
  }
  return +(chassis.position.x - x0).toFixed(2);
}

const out = {};
for (const v of ['buggy', 'truck'])
  for (const deg of [0, 12, 14, 16, 18, 20])
    for (const cell of [5, 2.5])
      for (const mode of v === 'truck' ? ['rwd', 'hill4x4'] : ['rwd'])
        (out[`${v}_${deg}deg_${cell}m_${mode}`] = run(v, deg, cell, mode));
console.log(JSON.stringify(out, null, 2));
```

- [ ] **Step 2: Rodar e sanidade do caso plano**

Run: `node scripts/probe-hillstart.mjs`
Expected: JSON no stdout; `buggy_0deg_5m_rwd` e `truck_0deg_5m_rwd` > 20 (plano = anda longe). Se 0°, der ~0 m, a orientação do Heightfield está errada — corrigir ANTES de ler o resto.

- [ ] **Step 3: Registrar o mapa medido**

Criar `docs/plans/2026-07-17-gaps-8-13-mapa.md` com: o JSON completo do probe, a tabela da Fase 0 deste plano e as duas leituras-chave: (a) menor grau em que `truck_*_rwd` anda <5 m (esperado: 14–16°) e ganho do `hill4x4`; (b) diferença 5 m × 2,5 m por grau (alimenta Task 8). Anotar a versão do cannon-es (`npm ls cannon-es`).

- [ ] **Step 4: Commit**

```bash
git add scripts/probe-hillstart.mjs docs/plans/2026-07-17-gaps-8-13-mapa.md
git commit -m "probe(veiculos): hill-start medido por rampa/grade/tracao (mapa dos gaps 8/12)"
```

---

### Task 2: Rede de segurança — fontes do Cover são independentes (node puro)

**Files:**
- Create: `test/cover-sources.test.js`

**Interfaces:**
- Consumes: `createCover()` de `js/cover.js` (ES module — importar com `await import`).
- Produces: prova de que `removeBySource('city')` (evento da cidade) não derruba o source `'campo'` que a Task 3 cria. É teste de caracterização: passa já — vira RED se alguém acoplar as fontes depois.

- [ ] **Step 1: Escrever o teste**

```js
/* Fontes do telhado climático são INDEPENDENTES: o evento de destruição da
   cidade remove 'city' e NÃO pode levar junto a cobertura do campo. */
'use strict';
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');

describe('cover.js — fontes campo × city', () => {
  it('campo sobrevive ao removeBySource(city); semântica de roofY vale', async () => {
    const { createCover } = await import('../js/cover.js');
    const C = createCover();
    C.addRoofRect({ x0: 0, x1: 4, z0: 0, z1: 4, roofY: 3, sourceId: 'campo' });
    C.addRoofRect({ x0: 10, x1: 14, z0: 0, z1: 4, roofY: 9, sourceId: 'city' });
    assert.equal(C.coverAt(2, 1, 2).covered, true, 'dentro da cabana');
    assert.equal(C.coverAt(2, 1, 2).sourceId, 'campo');
    assert.equal(C.coverAt(2, 5, 2).covered, false, 'EM CIMA do telhado = céu aberto');
    assert.equal(C.coverAt(12, 1, 2).covered, true, 'dentro do prédio');
    C.removeBySource('city');                        // evento da cidade
    assert.equal(C.coverAt(12, 1, 2).covered, false, 'prédio destruído descobre');
    assert.equal(C.coverAt(2, 1, 2).covered, true, 'campo intacto após o evento');
  });

  it('provider dinâmico tem precedência e recebe (x,y,z)', async () => {
    const { createCover } = await import('../js/cover.js');
    const C = createCover();
    const seen = [];
    C.setDynamicProvider((x, y, z) => { seen.push([x, y, z]); return { covered: true, sourceId: 'ship' }; });
    assert.equal(C.coverAt(7, 8, 9).sourceId, 'ship');
    assert.deepEqual(seen, [[7, 8, 9]]);
    C.setDynamicProvider(null);
    assert.equal(C.coverAt(7, 8, 9).covered, false);
  });
});
```

- [ ] **Step 2: Rodar**

Run: `node --test test/cover-sources.test.js`
Expected: `pass 2` (caracterização — `js/cover.js` já se comporta assim).

- [ ] **Step 3: Commit**

```bash
git add test/cover-sources.test.js
git commit -m "test(clima): fontes do cover independentes (campo sobrevive ao evento da cidade)"
```

---

### Task 3: Item 10 — telhado climático das estruturas do campo

**Files:**
- Modify: `js/structures.js` (funções `tower` ~linha 41 e `cabin` ~linha 59; retorno final do `createStructures`)
- Modify: `game.js` (logo após `buildCityCover();`, ~linha 1760)
- Test: `test/weather-cover.test.js` (porta 3234, arquivo existente)

**Interfaces:**
- Consumes: `Cover.addRoofRect({x0,x1,z0,z1,roofY,sourceId})` (`js/cover.js:15`).
- Produces: `Structures.fieldRoofs: Array<{x0,x1,z0,z1,roofY}>` — metadado PURO (zero RNG, zero colisão), registrado no `game.js` com `sourceId: 'campo'`. A Task 5 depende dele nos cenários de gota.

- [ ] **Step 1: Teste RED em `test/weather-cover.test.js`** (novo `it` antes da "rede de segurança")

```js
  it('dado o CAMPO (cabana/torre/ruína), então telhado climático certo — e imune ao evento da cidade', async () => {
    const r = await h.play(() => {
      const G = window.QA.G;
      const at = s => G.Cover.coverAt(s.x, G.heightAt(s.x, s.z) + 1.6, s.z);
      const cab = G.Structures.sites.find(s => s.type === 'cabana');
      const tor = G.Structures.sites.find(s => s.type === 'torre');
      const rui = G.Structures.sites.find(s => s.type === 'ruína');
      const antes = at(cab);
      G.Structures.city.destroy();
      const durante = at(cab);
      G.Structures.city.restore();
      return { antes, durante, torre: at(tor), ruina: at(rui),
        sobreLaje: G.Cover.coverAt(cab.x, G.heightAt(cab.x, cab.z) + 8, cab.z).covered };
    });
    assert.equal(r.antes.covered, true, 'cabana sem telhado climático: ' + JSON.stringify(r.antes));
    assert.equal(r.antes.sourceId, 'campo');
    assert.equal(r.torre.covered, true, 'embaixo da laje da torre sem cobertura');
    assert.equal(r.ruina.covered, false, 'ruína é ABERTA — cobertura fantasma');
    assert.equal(r.sobreLaje, false, 'acima do telhado tem que ser céu aberto');
    assert.equal(r.durante.covered, true, 'evento da cidade derrubou o telhado do CAMPO');
  });
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `node --test --test-concurrency=1 test/weather-cover.test.js`
Expected: FAIL no novo `it` (`cabana sem telhado climático`); os 7 `it` antigos passam.

- [ ] **Step 3: `js/structures.js` — coletar os retângulos (puro, sem RNG)**

No topo do `createStructures` (junto das outras coleções, ex.: perto de `const sites = []`):

```js
  const fieldRoofs = []; // telhados do CAMPO p/ o clima (js/cover.js) — metadado puro
```

Em `tower(cx, cz)`, logo após a laje `sbox(3.7, 0.28, 3.7, cx, y + H, cz, 0x8a6238);`:

```js
    fieldRoofs.push({ x0: cx - 1.85, x1: cx + 1.85, z0: cz - 1.85, z1: cz + 1.85, roofY: y + H + 0.14 });
```

Em `cabin(cx, cz, flip)`, logo após o forro `sbox(W + 0.8, 0.18, D + 0.8, cx, y + H + 0.35, cz, 0x6b4a2e);`:

```js
    fieldRoofs.push({ x0: cx - (W + 0.8) / 2, x1: cx + (W + 0.8) / 2,
      z0: cz - (D + 0.8) / 2, z1: cz + (D + 0.8) / 2, roofY: y + H + 0.44 });
```

No retorno final do `createStructures` (buscar `return { sites, walls, rayHit,`), acrescentar `fieldRoofs`:

```js
  return { sites, walls, rayHit, segBlocked, collide, FORT_POS, flames, smokeSpots, flags, city,
    cityMat, carSpots, enemyCamps, chestSpots, baseSites, heliSpot, bazookaSpot, towerTopY, NEXUS_INTERIOR,
    fieldRoofs };
```

Ruína e forte NÃO entram (abertos — decisão de design, coberta pelo assert da ruína).

- [ ] **Step 4: `game.js` — registrar com source próprio** (logo depois de `buildCityCover();`, antes do loop de `platforms`)

```js
// estruturas do CAMPO (torres/cabanas): telhado climático próprio — o evento
// da cidade remove só o source 'city'; 'campo' fica de pé
for (const r of Structures.fieldRoofs)
  Cover.addRoofRect({ x0: r.x0, x1: r.x1, z0: r.z0, z1: r.z1, roofY: r.roofY, sourceId: 'campo' });
```

- [ ] **Step 5: Rodar e ver passar**

Run: `node --test --test-concurrency=1 test/weather-cover.test.js`
Expected: PASS todos os `it` (8). Se flakear, re-rodar isolado 2–3×.

- [ ] **Step 6: Lint + commit**

```bash
npm run lint
git add js/structures.js game.js test/weather-cover.test.js
git commit -m "fix(clima): cabanas e torres do campo ganham telhado climatico proprio"
```

---

### Task 4: Item 9a — nave cobre pelo VOLUME real, não o mundo todo

**Files:**
- Modify: `ship-protocol.js` (junto de `worldToLocal`, ~linha 79; e o `return` do factory, ~linha 147)
- Modify: `br-game.js:693-694` (provider)
- Test: `test/ship-protocol.test.js` (node, existente)

**Interfaces:**
- Consumes: `worldToLocal(pose, world, out)` e `DIMS` de `ship-protocol.js`.
- Produces: `ShipProto.coversPoint(pose, x, y, z) -> boolean` — usado pelo provider do BR e testável em node puro.

- [ ] **Step 1: Teste RED em `test/ship-protocol.test.js`** (novo `it`, seguir o padrão de require/factory do arquivo)

```js
  it('coversPoint: casco cobre, fora do raio e acima do teto não', () => {
    const pose = { x: 100, y: 50, z: -30, yaw: 0.7, k: 0.5 };
    assert.equal(SP.coversPoint(pose, 100, 50, -30), true, 'centro da cabine');
    assert.equal(SP.coversPoint(pose, 117.9, 50, -30), true, 'borda do casco (r=17.9)');
    assert.equal(SP.coversPoint(pose, 119.5, 50, -30), false, 'fora do raio externo');
    assert.equal(SP.coversPoint(pose, 100, 55, -30), false, 'acima do teto');
    assert.equal(SP.coversPoint(pose, 100, 20, -30), true, 'sombra de chuva sob a nave');
  });
```

(`SP` = como o arquivo já instancia o protocolo; ajustar o nome à variável local do teste.)

- [ ] **Step 2: Rodar e ver falhar**

Run: `node --test test/ship-protocol.test.js`
Expected: FAIL `SP.coversPoint is not a function`.

- [ ] **Step 3: Implementar em `ship-protocol.js`** (depois de `worldToLocal`)

```js
  /* telhado climático da nave: a coordenada MUNDIAL está sob o casco?
     Raio EXTERNO (o casco inteiro faz sombra de chuva) + teto local. */
  const _cvp = [0, 0, 0];
  function coversPoint(pose, x, y, z) {
    _cvp[0] = x; _cvp[1] = y; _cvp[2] = z;
    worldToLocal(pose, _cvp, _cvp);
    return Math.hypot(_cvp[0], _cvp[2]) <= DIMS.outerRadius && _cvp[1] < DIMS.ceilingY + 0.6;
  }
```

Adicionar `coversPoint` ao objeto retornado pelo factory (linha do `walkRadius, routeYaw, poseAt, localToWorld, worldToLocal, ...`). Conferir se `DIMS` já é exportado; se não for, exportar junto (o teste usa dimensões via `coversPoint`, não via `DIMS`, então só exporte se o `br-game.js` já esperar `SHIP_DIMS = ShipProto.DIMS`).

- [ ] **Step 4: Rodar e ver passar**

Run: `node --test test/ship-protocol.test.js`
Expected: PASS (todos os `it` do arquivo).

- [ ] **Step 5: Trocar o provider em `br-game.js:693-694`**

```js
    // cabine/casco da nave = telhado climático REAL: testa a coordenada no
    // espaço LOCAL da nave (pose da fase, validada) — antes cobria o mundo
    // inteiro e apagava até a chuva visível pela janela
    if (G.Cover) G.Cover.setDynamicProvider((x, y, z) =>
      (S.phase === 'SHIP' && ShipProto.coversPoint(_shipPose, x, y, z))
        ? { covered: true, sourceId: 'ship' } : null);
```

- [ ] **Step 6: Regressão do BR + lint**

Run: `node --test --test-concurrency=1 test/ship-protocol.test.js test/ship.test.js test/ship-security.test.js test/br-cover.test.js && npm run lint`
Expected: PASS (flake browser → re-rodar isolado).

- [ ] **Step 7: Commit**

```bash
git add ship-protocol.js br-game.js test/ship-protocol.test.js
git commit -m "fix(clima): provider da nave cobre o volume real do casco, nao o mundo"
```

---

### Task 5: Item 9b — chuva/neve por gota (visível pela porta/janela)

**Files:**
- Modify: `js/env.js:140-186`
- Test: `test/weather-cover.test.js` (atualizar `countScript` e o `it` do prédio; estender o da cabana)

**Interfaces:**
- Consumes: `coverAt(x,y,z)` O(1) já injetado em `createEnv` (`js/env.js:12`); fontes `'campo'` da Task 3; `coversPoint` da nave via provider (Task 4).
- Produces: contrato novo de apresentação — malha de chuva SEMPRE ativa quando chove; gota individual some (escala 0) sse a posição MUNDIAL dela está coberta; `camExposure` continua mandando SÓ em som, relâmpago e transição.

- [ ] **Step 1: Atualizar o teste (RED)** — em `test/weather-cover.test.js`:

Substituir `countScript` (linhas 19-37) por versão que separa gotas visíveis de gotas em posição coberta:

```js
  /* conta gotas visíveis (escala>0.01) num raio da câmera e quantas delas
     estão em posição COBERTA (bug: chuva "dentro" de teto) */
  const countScript = () => {
    const G = window.QA.G, MP = window.QA.MP, THREE = MP.THREE;
    const m4 = new THREE.Matrix4(), p = new THREE.Vector3(), q = new THREE.Quaternion(), s = new THREE.Vector3();
    window.__QA_countDrops = (frames, radius) => {
      let vis = 0, cobertas = 0;
      const rain = MP.scene.getObjectByName('rainFx');
      for (let f = 0; f < frames; f++) {
        window.QA.tick(1);
        if (!rain || !rain.visible) continue;
        const cam = G.camera.getWorldPosition(new THREE.Vector3());
        for (let i = 0; i < rain.count; i++) {
          rain.getMatrixAt(i, m4);
          m4.decompose(p, q, s);
          if (s.y <= 0.01 || p.distanceTo(cam) >= radius) continue;
          vis++;
          if (G.Cover.coverAt(p.x, p.y, p.z).covered) cobertas++;
        }
      }
      return { vis, cobertas };
    };
  };
```

No 1º `it` (pradaria): `const fora = window.__QA_countDrops(60, 40);` vira `const fora = window.__QA_countDrops(60, 40).vis;` (resto igual).

No `it` do prédio (linha 59): trocar a coleta e os asserts finais por:

```js
      const d = window.__QA_countDrops(300, 30);
      return { d, exposure: G.Env.camExposure, nivel: MP.SFX.rainLevel(),
        cobertura: G.Cover.coverAt(x, G.groundAt(x, z, 5) + 1.6, z) };
```

```js
    assert.equal(r.cobertura.covered, true, 'coverAt não detecta o prédio: ' + JSON.stringify(r.cobertura));
    assert.ok(r.exposure < 0.1, `exposure alto dentro do prédio: ${r.exposure}`);
    assert.equal(r.d.cobertas, 0, `${r.d.cobertas} gotas DENTRO de volume coberto`);
    assert.ok(r.d.vis > 50, `chuva lá fora sumiu com a câmera coberta (vis ${r.d.vis}) — regressão do "some inteira"`);
    assert.ok(r.nivel <= 0.05 * 0.3 + 1e-9, `som interno alto: ${r.nivel} (externo ~0.05)`);
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `node --test --test-concurrency=1 test/weather-cover.test.js`
Expected: FAIL no assert `vis > 50` (hoje a malha some inteira: `vis === 0`).

- [ ] **Step 3: Implementar em `js/env.js`** — substituir o bloco de chuva (linhas 140-161) por:

```js
    // malha de chuva SEMPRE ativa quando chove: quem esconde gota é a
    // POSIÇÃO dela (coberta ⇒ escala 0), não a exposição da câmera —
    // pela porta/janela a chuva de fora continua visível. camExposure
    // segue mandando só em som, relâmpago e transição (acima).
    const showRain = weather === 'chuva' && weatherK > 0.04;
    rainMesh.visible = showRain;
    rainMesh.material.opacity = 0.42;
    if (showRain) {
      rainMesh.count = Math.max(1, Math.floor(RN * weatherK));
      for (let i = 0; i < rainMesh.count; i++) {
        const p = rainP[i];
        p.y -= (34 + (p.spd || 0)) * dt;
        if (p.y < -2) {
          p.y = 24; p.x = rand(-22, 22); p.z = rand(-22, 22);
          p.spd = rand(0, 7); p.len = rand(0.7, 1.25); // hastes variadas, nada de "macarrão" idêntico
        }
        // classificação POR FRAME e por gota: consulta O(1) na grade de
        // telhados (~450/frame), zero raycast; pega gota que ATRAVESSA teto
        const hidden = dropCovered(cp.x + p.x, cp.y + p.y - 8, cp.z + p.z);
        _dummy2.position.set(cp.x + p.x, cp.y + p.y - 8, cp.z + p.z);
        // inclinação segue o VENTO compartilhado (mesma direção da grama)
        _dummy2.rotation.set(wind.dirZ * 0.09, 0, -wind.dirX * 0.09);
        _dummy2.scale.set(1, hidden ? 0 : (p.len || 1), 1);
        _dummy2.updateMatrix();
        rainMesh.setMatrixAt(i, _dummy2.matrix);
      }
      rainMesh.instanceMatrix.needsUpdate = true;
    }
```

E o bloco de neve (162-186), mesma cirurgia: `const showSnow = weather === 'neve' && weatherK > 0.04;`, remover `p.hidden` do respawn e classificar por frame:

```js
        const hidden = dropCovered(
          cp.x + p.x + Math.sin(p.ph) * 0.9 + wind.dirX * p.ph * 0.02,
          cp.y + p.y - 6,
          cp.z + p.z + Math.cos(p.ph * 0.8) * 0.9 + wind.dirZ * p.ph * 0.02);
```

com `_dummy2.scale.setScalar(hidden ? 0 : 1);`. (O campo `p.hidden` deixa de existir nos dois — atenção ao lint `no-unused-vars`.)

- [ ] **Step 4: Rodar e ver passar**

Run: `node --test --test-concurrency=1 test/weather-cover.test.js`
Expected: PASS os 8 `it` — inclusive `cobertas === 0` (garante que a mudança não deixou gota "chovendo dentro" da cabana/prédio/nave).

- [ ] **Step 5: Regressão de clima + lint**

Run: `node --test --test-concurrency=1 test/climate.test.js test/weather-cover.test.js && npm run lint`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add js/env.js test/weather-cover.test.js
git commit -m "fix(clima): chuva/neve por gota — visivel pela porta, zero gotas sob teto"
```

---

### Task 6: Item 8 — 4×4 de arranque do caminhão (hill-start ≥14°)

**Files:**
- Create: `test/car-hillstart.test.js` (porta FIXA **3236**)
- Modify: `js/car.js:294-299` (config) e `js/car.js:338-343` (aplicação de força)

**Interfaces:**
- Consumes: caminho AWD existente (`js/car.js:338-339`), `_cv1` = forward do chassi já calculado (`js/car.js:321`), `wheelInfos[i].isInContact` do cannon-es; alvos numéricos do probe da Task 1.
- Produces: flag de config `hillAwd: true` (só caminhão) e contrato documentado: **hillStartSafe ≈ 16°** (arranca parado), **driveable = 20°** (com embalo) — total de força inalterado (2×3600), acima de 15 km/h é 100% traseira (drift preservado).

- [ ] **Step 1: Teste RED** — criar `test/car-hillstart.test.js`:

```js
/* ================================================================
   QA — HILL-START: arrancar PARADO em rampa íngreme (contrato novo).
   hillStartSafe: caminhão arranca parado em 13,5–16,5° e anda ≥8 m em
   6 s (4x4 de arranque). driveable mundial segue 20° COM embalo.
   Guarda-corpo: reta plana continua chegando a ≥55 km/h (a distribuição
   de força não pode castrar a traseira nem mudar o total).
   ================================================================ */
'use strict';
const { describe, it, before, after } = require('node:test');
const assert = require('node:assert');
const { CHROME, bootGame } = require('./helpers/harness.js');

describe('Hill-start do caminhão (Chrome headless)', { skip: !CHROME && 'Chrome não encontrado' }, () => {
  let h;
  before(async () => { h = await bootGame({ port: 3236 }); });
  after(async () => { if (h) await h.close(); });

  /* Copiar de test/car-terrain-traversal.test.js: o helper __QA_drive
     (teleporte do veículo + acelerador por N s + telemetria) e o esqueleto
     do __QA_findCorridor — aqui com predicado de RAMPA DE SUBIDA:
     faixa 13,5–16,5°, altura crescendo ao longo do yaw. */
  const setupScript = () => {
    const G = window.QA.G, MP = window.QA.MP;
    window.__QA_findRamp = (minDeg, maxDeg, len = 18) => {
      for (let k = 0; k < 12000; k++) {
        const x = ((k * 137.51) % 960) - 480, z = ((k * 91.17) % 960) - 480;
        const yaw = (k % 16) * Math.PI / 8;
        const dx = Math.cos(yaw), dz = -Math.sin(yaw);
        const lx = -dz, lz = dx;
        let ok = true;
        for (let d = 0; ok && d <= len; d += 1) {
          for (const off of [-2, 0, 2]) {
            const su = G.surfaceAt(x + dx * d + lx * off, z + dz * d + lz * off);
            if (!su.driveable || su.slopeDegrees < minDeg || su.slopeDegrees > maxDeg ||
                su.height < MP.WATER_LEVEL + 1) { ok = false; break; }
          }
          if (!ok) break;
          for (const o of G.obstaclesNear(x + dx * d, z + dz * d)) {
            if (o.category !== 'softVegetation' &&
                Math.hypot(x + dx * d - o.x, z + dz * d - o.z) < o.r + 3.2) { ok = false; break; }
          }
          const dc = Math.hypot(x + dx * d + 340, z + dz * d - 130), dv = Math.hypot(x + dx * d - 420, z + dz * d + 420);
          if (dc < 130 || dv < 135) ok = false;
        }
        // SUBIDA de verdade: ganho de altura coerente com ≥13° ao longo do corredor
        if (ok && (G.heightAt(x + dx * len, z + dz * len) - G.heightAt(x, z)) < Math.tan(13 * Math.PI / 180) * len * 0.8) ok = false;
        if (ok) return { x, z, yaw };
      }
      return null;
    };
  };

  it('caminhão arranca PARADO em rampa 13,5–16,5° e anda ≥8 m em 6 s', async () => {
    const r = await h.play((src) => {
      eval(src)();
      window.QA.reset();
      const ramp = window.__QA_findRamp(13.5, 16.5);
      if (!ramp) return { skip: 'sem rampa no seed' };
      // __QA_drive: mesmo helper do car-terrain-traversal (copiado no setup)
      return { ramp, ...window.__QA_drive('CAMINHÃO MILITAR', ramp, 6, 1 / 60, true) };
    }, `(${setupScript})`);
    if (r.skip) return; // seed sem rampa na faixa: aceito (mapa é procedural)
    assert.ok(r.dist >= 8, `caminhão estolou na rampa ${JSON.stringify(r.ramp)}: ${r.dist} m em 6 s`);
  });

  it('guarda-corpo: reta plana ainda chega a ≥55 km/h (traseira não castrada)', async () => {
    const r = await h.play(() => {
      const flat = window.__QA_findRamp(0, 3, 40);
      if (!flat) return { skip: true };
      return { ...window.__QA_drive('CAMINHÃO MILITAR', flat, 6, 1 / 60, true) };
    });
    if (r.skip) return;
    assert.ok(r.kmhMax >= 55, `velocidade de pico caiu: ${r.kmhMax} km/h`);
  });

  it('rede de segurança: nenhum pageerror', () => {
    assert.deepEqual(h.pageErrors, [], 'erros: ' + h.pageErrors.join(' | '));
  });
});
```

Nota de implementação: `__QA_drive` referido acima é o helper REAL de `test/car-terrain-traversal.test.js` — copiar a implementação para o `setupScript` deste arquivo (com o parâmetro de veículo por NOME, partida com velocidade zero e retorno `{ dist, kmhMax }`). Não inventar API nova: adaptar a existente.

- [ ] **Step 2: Rodar e ver falhar**

Run: `node --test --test-concurrency=1 test/car-hillstart.test.js`
Expected: FAIL no 1º `it` com `dist` pequeno. **Nota do probe (Task 1, medido):** rampa UNIFORME alinhada não estola o caminhão em grau nenhum (0–20° ⇒ ≥114 m) — sem demanda lateral o círculo de fricção não satura. O estol real exige o terreno do jogo (rugosidade fbm + componente lateral do corredor com yaw ≠ gradiente). Se o RED não disparar na faixa 13,5–16,5°: (a) afrouxar para 13–17°; (b) preferir corredores com yaw desalinhado do gradiente (subida em diagonal). Se AINDA assim o caminhão arrancar em TODOS os corredores achados, pare e reporte — o gap 8 pode já não reproduzir no HEAD e a task vira só o contrato de teste, sem mexer em js/car.js.

- [ ] **Step 3: Implementar em `js/car.js`**

Na config do caminhão (linha 294-299), adicionar a flag:

```js
    force: 3600, steer: 0.45, brake: 55, maxKmh: 84, grip: 2.0, engine: 'truck', // idem buggy: 1.6 estolava em hill-start ≥14°
    hillAwd: true, // 4x4 SÓ no arranque em subida (ver update) — drift intacto
```

Substituir o bloco de aplicação de força (linhas 338-343) por:

```js
        if (v.cfg.awd) { // tração integral: divide a força nas 4 rodas
          for (let i = 0; i < 4; i++) v.vehicle.applyEngineForce(DRIVE_SIGN * force * 0.5, i);
        } else {
          /* 4x4 de ARRANQUE (hillAwd): em subida real (~≥10°), quase parado
             e acelerando pra frente, metade da força migra pro eixo
             dianteiro — o TOTAL não muda (2×force), só a distribuição.
             O limite de atrito por roda (círculo de fricção do cannon) é
             compartilhado lateral+longitudinal: em rampa, 2 rodas saturam;
             4 rodas somam o dobro de limite útil. Acima de 15 km/h volta
             100% traseira: drift e frenagem calibrados não mudam. */
          let awdK = 0;
          if (v.cfg.hillAwd && force > 0 && _cv1.y > 0.18 && kmh < 15) {
            let contacts = 0;
            for (let i = 0; i < 4; i++) if (v.vehicle.wheelInfos[i].isInContact) contacts++;
            if (contacts >= 3) awdK = Math.min(1, (15 - kmh) / 5);
          }
          const perFront = force * 0.5 * awdK, perRear = force * (1 - 0.5 * awdK);
          v.vehicle.applyEngineForce(DRIVE_SIGN * perFront, 0);
          v.vehicle.applyEngineForce(DRIVE_SIGN * perFront, 1);
          v.vehicle.applyEngineForce(DRIVE_SIGN * perRear, 2);
          v.vehicle.applyEngineForce(DRIVE_SIGN * perRear, 3);
        }
```

(`_cv1` já contém o forward do chassi — linha 321; `_cv1.y > 0.18` ≈ nariz ≥10,4° pra cima. Ré/descida: `force <= 0` ⇒ `awdK = 0` ⇒ comportamento idêntico ao atual.)

- [ ] **Step 4: Rodar e ver passar**

Run: `node --test --test-concurrency=1 test/car-hillstart.test.js`
Expected: PASS 3 `it`.

- [ ] **Step 5: Regressão de veículos (drift/travessia/assentamento)**

Run: `node --test --test-concurrency=1 test/car-terrain-traversal.test.js test/car-settle.test.js test/car-remote-wheels.test.js && npm run lint`
Expected: PASS (flake browser → protocolo isolado 2–3×).

- [ ] **Step 6: Commit**

```bash
git add js/car.js test/car-hillstart.test.js
git commit -m "fix(veiculos): 4x4 de arranque do caminhao em subida — hill-start ate ~16 sem mexer em forca total"
```

---

### Task 7: Item 11 — trilhas de pneu na grama (temporárias, por lâmina)

**Files:**
- Modify: `js/grass.js` (uniform + atributo + `stampTrack` + reset na reciclagem)
- Modify: `js/car.js` (dep opcional `stampTrack`, chamada no branch dirigido)
- Modify: `game.js:1634` (injeção da dep)
- Test: `test/grass-decor.test.js` (estender)

**Interfaces:**
- Consumes: chunks instanciados de `js/grass.js` (`fillChunk`, atributos `aPhase`/`aTint`, `uniforms.uTime`); `wheelInfos[i].raycastResult.hitPointWorld` + `isInContact` do RaycastVehicle.
- Produces: `Grass.stampTrack(x0, z0, x1, z1)` — marca lâminas num corredor de meia-largura 0,42 m; shader achata e a marca some em ~10 s; `debugChunkBytes` passa a devolver `tr` (array `aTrack`) para QA.

- [ ] **Step 1: Teste RED** — novo `it` em `test/grass-decor.test.js` (padrão do arquivo):

```js
  it('trilha de pneu: amassa lâminas SÓ no corredor e zera no chunk reciclado', async () => {
    const r = await h.play(() => {
      const G = window.QA.G;
      G.Grass.stampTrack(1, -3, 1, 3);           // segmento dentro do chunk (0,0)
      const b = G.Grass.debugChunkBytes(0, 0);
      const marcadas = b.tr.filter(v => v > -1).length;
      const total = b.tr.length;
      G.Grass.refreshAll();                       // reciclagem/refill limpa a trilha
      const depois = G.Grass.debugChunkBytes(0, 0).tr.filter(v => v > -1).length;
      return { marcadas, total, depois };
    });
    assert.ok(r.marcadas > 0, 'nenhuma lâmina marcada no corredor');
    assert.ok(r.marcadas < r.total * 0.2, `trilha larga demais: ${r.marcadas}/${r.total}`);
    assert.equal(r.depois, 0, 'trilha sobreviveu à reciclagem do chunk');
  });
```

Run: `node --test --test-concurrency=1 test/grass-decor.test.js`
Expected: FAIL `G.Grass.stampTrack is not a function`.

- [ ] **Step 2: `js/grass.js` — uniform e shader**

Em `uniforms` (após `uPatchRadius`):

```js
    uTrackFade:   { value: 10.0 },  // segundos até a trilha de pneu sumir
```

No vertex shader: junto dos outros `attribute`, adicionar

```glsl
      attribute float aTrack;
      uniform float uTrackFade;
```

e no `main`, logo após `float hh = h * h;`:

```glsl
        // trilha de pneu: lâmina amassada que levanta em uTrackFade segundos
        float trackK = clamp(1.0 - (uTime - aTrack) / uTrackFade, 0.0, 1.0);
        transformed.y *= 1.0 - trackK * 0.85;
```

- [ ] **Step 3: `js/grass.js` — atributo, raízes e reset na reciclagem**

Em `makeChunk`, junto de `aPhase`/`aTint`:

```js
    geo.setAttribute('aTrack', new THREE.InstancedBufferAttribute(new Float32Array(PER_CHUNK).fill(-1e4), 1));
```

e o chunk ganha as raízes locais: `const chunk = { mesh, cx: 99999, cz: 99999, roots: new Float32Array(PER_CHUNK * 2) };`

Em `fillChunk`: no início (chunk reciclado nasce limpo):

```js
    const track = chunk.mesh.geometry.attributes.aTrack;
    track.array.fill(-1e4);
    track.needsUpdate = true;
```

e dentro do loop, logo após calcular `lx`/`lz`:

```js
      chunk.roots[i * 2] = lx; chunk.roots[i * 2 + 1] = lz;
```

- [ ] **Step 4: `js/grass.js` — `stampTrack` + export + QA**

Antes do `return` final:

```js
  /* trilha de pneu: marca as lâminas num corredor segmento±TRACK_HW com o
     timestamp atual — o shader achata e devolve em uTrackFade s. Runtime
     puro: NENHUM rand, nenhum corpo físico, memória fixa por chunk. Só
     chunks que cruzam o bbox do segmento pagam o loop; needsUpdate sobe
     o buffer daquele chunk (~4 KB), nunca o tapete inteiro. */
  const TRACK_HW = 0.42;
  function stampTrack(x0, z0, x1, z1) {
    const minX = Math.min(x0, x1) - TRACK_HW, maxX = Math.max(x0, x1) + TRACK_HW;
    const minZ = Math.min(z0, z1) - TRACK_HW, maxZ = Math.max(z0, z1) + TRACK_HW;
    for (const ch of chunks) {
      const ox = ch.cx * SIZE, oz = ch.cz * SIZE;
      if (ox + SIZE / 2 < minX || ox - SIZE / 2 > maxX || oz + SIZE / 2 < minZ || oz - SIZE / 2 > maxZ) continue;
      const track = ch.mesh.geometry.attributes.aTrack;
      const ax = x0 - ox, az = z0 - oz;
      const abx = (x1 - ox) - ax, abz = (z1 - oz) - az;
      const ab2 = abx * abx + abz * abz || 1e-6;
      let dirty = false;
      for (let i = 0; i < PER_CHUNK; i++) {
        const px = ch.roots[i * 2] - ax, pz = ch.roots[i * 2 + 1] - az;
        let t = (px * abx + pz * abz) / ab2;
        t = t < 0 ? 0 : t > 1 ? 1 : t;
        const dx = px - abx * t, dz = pz - abz * t;
        if (dx * dx + dz * dz > TRACK_HW * TRACK_HW) continue;
        track.setX(i, uniforms.uTime.value);
        dirty = true;
      }
      if (dirty) track.needsUpdate = true;
    }
  }
```

No `return`: `return { update, material, PATCH_RADIUS, refreshAll, debugSample, debugChunkBytes, stampTrack };`

Em `debugChunkBytes`, acrescentar ao objeto devolvido:

```js
      tr: Array.from(g.attributes.aTrack.array),
```

- [ ] **Step 5: `js/car.js` — origem das marcas (rodas traseiras reais)**

Na desestruturação de deps (linha 7), acrescentar `stampTrack = null`. No branch `driven` do `update`, logo após o bloco de `applyEngineForce`:

```js
        /* trilha na grama: segmento anterior→atual de cada roda TRASEIRA em
           contato real, só em movimento (>2 m/s). Onde não há lâmina
           (asfalto/água/deserto: escala ~0 na geração) a marca é inócua. */
        if (stampTrack && Math.abs(fwdSpeed) > 2) {
          if (!v._trk) v._trk = [null, null];
          for (let k = 0; k < 2; k++) {
            const info = v.vehicle.wheelInfos[2 + k];
            if (!info.isInContact) { v._trk[k] = null; continue; }
            const hp = info.raycastResult.hitPointWorld;
            const prev = v._trk[k];
            if (!prev) { v._trk[k] = { x: hp.x, z: hp.z }; continue; }
            const ddx = hp.x - prev.x, ddz = hp.z - prev.z;
            if (ddx * ddx + ddz * ddz > 0.16) {          // passo de 0,4 m
              stampTrack(prev.x, prev.z, hp.x, hp.z);
              prev.x = hp.x; prev.z = hp.z;
            }
          }
        } else if (v._trk) v._trk = null;
```

Em `game.js:1634`, injetar: `..., keys, CITY, stampTrack: Grass.stampTrack });` (a `Grass` nasce na linha 288 — antes do `Car`).

- [ ] **Step 6: Rodar e ver passar**

Run: `node --test --test-concurrency=1 test/grass-decor.test.js`
Expected: PASS (incluindo o `it` novo).

- [ ] **Step 7: Regressão de grama/veículo + lint**

Run: `node --test --test-concurrency=1 test/grass-decor.test.js test/car-terrain-traversal.test.js && npm run lint`
Expected: PASS. O contrato de determinismo da grama (`debugChunkBytes` de matriz/fase/tint) NÃO muda — `aTrack` é estado de runtime, resetado no refill.

- [ ] **Step 8: Commit**

```bash
git add js/grass.js js/car.js game.js test/grass-decor.test.js
git commit -m "feat(grama): trilhas temporarias de pneu por lamina instanciada (some em ~10s)"
```

---

### Task 8: Item 12 — A/B de resolução 2,5 m: instrumentar e decidir (não adotar)

**Files:**
- Modify: `game.js:78-81` (param `?segs=`)
- Modify: `docs/plans/2026-07-17-gaps-8-13-mapa.md` (protocolo + resultado físico do probe)

**Interfaces:**
- Consumes: `buildHeightGrid(worldSize, segs)` (`js/terrain.js:77`), `CFG.TERRAIN_SEGS` (`js/config.js:13`), dados 5 m × 2,5 m do probe (Task 1).
- Produces: modo de medição `?segs=440` (célula 2,5 m) e protocolo de decisão registrado. **Adoção NÃO é deste plano** — ver restrição abaixo.

**Restrição (do mapa, Fase 0):** `heightAt` da grade alimenta a geração das structures (`game.js:338`) ⇒ mudar `segs` muda o layout do mundo pro mesmo seed. Logo: 2,5 m NUNCA pode ser opção por cliente/gráficos — adoção real = trocar `CFG.TERRAIN_SEGS` pra TODO MUNDO (cliente, bots, servidor que reconstrói) numa mudança única, com re-baseline dos testes de mundo. O `?segs=` é ferramenta de MEDIÇÃO solo.

- [ ] **Step 1: Param de medição em `game.js`** — substituir a linha 81 (`buildHeightGrid(CFG.WORLD_SIZE);`) por:

```js
/* A/B de resolução (gap 12): ?segs=440 → célula 2,5 m. MUDA o layout do
   mundo pro mesmo seed (worldgen lê heightAt da grade) — ferramenta de
   MEDIÇÃO solo; adoção real = mudar CFG.TERRAIN_SEGS pra todos de uma vez. */
const _segsQA = +(new URLSearchParams(location.search).get('segs') || 0);
if (_segsQA >= 55 && _segsQA <= 880) {
  CFG.TERRAIN_SEGS = _segsQA;
  console.warn(`[A/B] TERRAIN_SEGS=${_segsQA} — mundo diverge do canônico`);
}
buildHeightGrid(CFG.WORLD_SIZE, CFG.TERRAIN_SEGS);
```

(Sem `?segs`, nada muda: 220 ≡ default `worldSize/5`. A malha visual em `game.js:229` e o Heightfield em `game.js:194-204` já leem `CFG.TERRAIN_SEGS` — passam a acompanhar sozinhos.)

- [ ] **Step 2: Sanidade — o mundo canônico não mudou**

Run: `node --test --test-concurrency=1 test/terrain-surface.test.js test/terrain-physics.test.js && npm run lint`
Expected: PASS sem mexer em baseline nenhum (portas 3230/3232 — o boot sem `?segs` usa 220).

- [ ] **Step 3: Protocolo de medição no mapa** — acrescentar em `docs/plans/2026-07-17-gaps-8-13-mapa.md`:

```markdown
## A/B 5 m × 2,5 m — protocolo (rodar no Chrome REAL do Will)

Mesmo seed, mesmos veículos, mesma rota de câmera, mesmo clima (Env.weather
fixado), 3 min por lado: `/?qa=1` vs `/?qa=1&segs=440`.

Coletar por lado (snippet no console):
    (() => { const N = 1800, dts = new Float32Array(N); let i = 0, last = performance.now();
      const loop = () => { const t = performance.now(); dts[i % N] = t - last; last = t;
        if (++i < N) requestAnimationFrame(loop); else { const a = [...dts].sort((x, y) => x - y);
          console.log('frame p50/p95 (ms):', a[900].toFixed(2), a[1710].toFixed(2),
            'tris:', window.QA?.MP?.renderer?.info.render.triangles); } };
      requestAnimationFrame(loop); })();

Mais: hill-start nas mesmas rampas do probe (dirigindo), contagem de falso
travamento na rota, memória (performance.memory.usedJSHeapSize).

GATE de adoção: melhoria física do probe segue aparecendo no browser E
frame p95 não piora >15% E tris/memória aceitáveis no PC do Will. Qualquer
adoção = mudança única de CFG.TERRAIN_SEGS pra todos + re-baseline
(drawCalls 450 da torre, testes de terreno) — NUNCA opção por cliente.
NUNCA separar terreno visual de físico (recria carro enterrado/flutuando).
```

- [ ] **Step 4: Commit**

```bash
git add game.js docs/plans/2026-07-17-gaps-8-13-mapa.md
git commit -m "chore(terreno): ?segs= p/ A-B de resolucao 2,5m + protocolo de decisao (medicao, sem adocao)"
```

---

### Task 9: Item 13 (DAY_LEN) + fechamento do backlog

**Files:**
- Modify: `docs/2026-07-17-gaps-backlog.md` (linhas dos itens 8–13)
- Modify: `docs/plans/2026-07-17-gaps-8-13-mapa.md` (nota do item 13)

- [ ] **Step 1: Registrar a decisão do item 13 (sem código)**

No mapa: `DAY_LEN mantido em 480 (js/climate.js:9) — decisão consciente: dia ~6:27 casa com a direção "Vice City"/golden hour; se o playtest do SOLO acusar ritmo lento, a mudança é um todRate específico do modo solo (nunca DAY_LEN duplicado — evita dois relógios).` Nada de código: YAGNI até o playtest reclamar.

- [ ] **Step 2: Atualizar a tabela do backlog** — em `docs/2026-07-17-gaps-backlog.md`, marcar:

- 8: `resolvido — 4x4 de arranque (hillAwd) em js/car.js; contrato hillStartSafe~16°/driveable 20° em test/car-hillstart.test.js`
- 9: `resolvido — gota por posição mundial (env.js) + volume real da nave (coversPoint)`
- 10: `resolvido — fieldRoofs (torre/cabana) com source 'campo'`
- 11: `resolvido — Grass.stampTrack + aTrack no shader`
- 12: `instrumentado — probe + ?segs=440 + protocolo; DECISÃO pendente de medição no PC do Will (gate no mapa)`
- 13: `fechado — mantido 480, gatilho = playtest do solo`

- [ ] **Step 3: Suíte completa (validação final)**

Run: `npm test`
Expected: verde (~10 min). Falha browser → protocolo de flake (isolado 2–3×) antes de reabrir qualquer task.

- [ ] **Step 4: Commit**

```bash
git add docs/2026-07-17-gaps-backlog.md docs/plans/2026-07-17-gaps-8-13-mapa.md
git commit -m "docs: fecha gaps 8-13 no backlog (12 instrumentado, 13 mantido por decisao)"
```

---

## Self-Review (executada na escrita do plano)

- **Cobertura:** 8→T1+T6, 9→T4+T5, 10→T3, 11→T7, 12→T1+T8, 13→T9; "mapear antes" → Fase 0 + T1; eventos da cidade → T2/T3 (asserts explícitos de destroy/restore); nave → T4.
- **Consistência de nomes entre tasks:** `fieldRoofs` (T3), `coversPoint` (T4→T5 via provider), `stampTrack` (T7 grass→car→game), `hillAwd` (T6), `?segs` (T8). `__QA_drive` é cópia do helper existente — instrução explícita de adaptar, não inventar.
- **Riscos deixados anotados:** transformação do Heightfield no probe (passo de sanidade 0° antes de ler dados); faixa de rampa pode não existir no seed (skip explícito + afrouxar faixa); custo de 800 `coverAt`/frame é O(1) por consulta — se p95 reclamar, amortizar é follow-up, não pré-otimização.

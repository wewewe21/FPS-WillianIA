# Solução final dos gaps 8 e 12 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans para executar task a task. Steps usam checkbox (`- [ ]`).

**Goal:** Fechar de verdade os dois itens que a rodada anterior deixou abertos: o encalhe de barriga do caminhão (gap 8, causa-raiz MEDIDA em `test/car-hillstart.test.js`) e a decisão do A/B de resolução 2,5 m (gap 12), medindo nesta máquina (Chrome real com GPU — sem depender de terceiros).

**Architecture:** Gap 8 = remodelar SÓ a parte de baixo do collider do caminhão em duas caixas (casco alto de comprimento total + barriga curta entre eixos), criando ângulo de ataque/saída real. Verificado por leitura de código: a forma do cannon só interage com o mundo estático — atropelamento é distância+velocidade (`js/enemies.js:310`), colisão player-veículo é círculo por `cfg.half[0]/[2]` (`game.js:1033-1040`), balas não consultam o box. Gap 12 = medir com o protocolo já commitado no mapa, no Chrome desta máquina, e aplicar o gate.

**Tech Stack:** cannon-es 0.20 (Body multi-shape), node:test + harness puppeteer, claude-in-chrome p/ a medição A/B.

## Global Constraints

- Regras herdadas do plano anterior (`docs/plans/2026-07-17-gaps-8-13-plano.md`): RNG seedado intocado; destruição da cidade intencional; cover só apresentação; portas fixas (3236 = hill-start) com `--test-concurrency=1`; flake = isolado 2-3×; porta 3000 intocada; lint limpo; nunca stagear `br-rank.json`/`boss-castle.v1.glb`.
- **Sem upstream:** projeto próprio — commit normal na `refatoracao`, sem fluxo de PR externo.
- Gap 8: PROIBIDO força vertical, teleporte, unstuck, grip/força/maxKmh. A mudança é GEOMETRIA do collider, e só do caminhão (`cfg.shapes` opcional — buggy/sport ficam na caixa única).
- O encalhe de CRISTA (entre eixos) é relevo legítimo ("pendurado = dá ré", doutrina do `car-terrain-traversal`) — a caixa da barriga PRESERVA esse comportamento; nunca removê-la.
- Medição A/B: máquina QUIETA (nenhum teste/subagente rodando em paralelo — CPU concorrente contamina p95).

---

### Task 1: Gap 8 — collider do caminhão em duas caixas

**Files:**
- Modify: `js/car.js` (`createPhysics` linha ~61; `CFG_TRUCK` linhas ~294-299)
- Modify: `test/car-hillstart.test.js` (it da rampa sai de `todo` e vira contrato real)

**Interfaces:**
- Consumes: `chassisBody.addShape(shape, offset)` do cannon-es (multi-shape já suportado; inércia recalculada por `updateMassProperties` interno).
- Produces: `cfg.shapes?: Array<{half:[x,y,z], off:[x,y,z]}>` — `off` relativo ao CENTRO da caixa histórica (y = `comDrop`). Ausente ⇒ caixa única atual (buggy/sport bit-idênticos).

Geometria (espaço do chassi; caixa histórica do caminhão spans y ∈ [−0.25, +0.85], eixos em x = +1.673 e −0.812):

| Caixa | half | off | spans |
|---|---|---|---|
| casco (footprint total) | [2.7, 0.40, 1.05] | [0, 0.15, 0] | y ∈ [+0.05, +0.85] |
| barriga (entre eixos) | [1.0, 0.15, 0.95] | [0.4, −0.40, 0] | y ∈ [−0.25, +0.05], x ∈ [−0.6, +1.4], z ∈ [−0.95, +0.95] |

Balanços dianteiro/traseiro sobem 0,30 m (ângulo de ataque/saída); o fundo original continua existindo SÓ entre os eixos (crista continua pendurando).

- [ ] **Step 1: RED — reativar o contrato**

Em `test/car-hillstart.test.js`: remover o `{ todo: ... }` do it da rampa (volta a assert normal `dist >= 8`); atualizar o cabeçalho: manter o histórico dos 2 mecanismos descartados e registrar a solução (collider 2 caixas). Rodar:

Run: `node --test --test-concurrency=1 test/car-hillstart.test.js`
Expected: FAIL com dist ~1-3 m no corredor de estol (o RED conhecido).

- [ ] **Step 2: Implementar em `js/car.js`**

Substituir a linha 61 (`chassisBody.addShape(new CANNON.Box(...), new CANNON.Vec3(0, drop, 0));`) por:

```js
    /* casco físico: caixa única (padrão) ou composição por cfg.shapes —
       [{half, off}] com off relativo ao CENTRO da caixa histórica (y=comDrop).
       Caminhão: casco alto de comprimento total + BARRIGA curta entre eixos —
       ângulo de ataque/saída real; encalhe de CRISTA (entre eixos) continua
       existindo de propósito (relevo legítimo, dá ré). */
    const shapes = cfg.shapes || [{ half: cfg.half, off: [0, 0, 0] }];
    for (const s of shapes)
      chassisBody.addShape(new CANNON.Box(new CANNON.Vec3(s.half[0], s.half[1], s.half[2])),
        new CANNON.Vec3(s.off[0], drop + s.off[1], s.off[2]));
```

Em `CFG_TRUCK`, adicionar (depois da linha do `force`):

```js
    // encalhe de barriga MEDIDO (test/car-hillstart): a caixa única tocava o
    // terreno 348/360 frames na diagonal ≥14°. Duas caixas: casco y[0.05..0.85]
    // no footprint todo + barriga y[-0.25..0.05] SÓ entre eixos.
    shapes: [
      { half: [2.7, 0.40, 1.05], off: [0, 0.15, 0] },
      { half: [1.0, 0.15, 0.95], off: [0.4, -0.40, 0] },
    ],
```

- [ ] **Step 3: GREEN**

Run: `node --test --test-concurrency=1 test/car-hillstart.test.js`
Expected: 3 it PASS (rampa ≥8 m, reta ≥55 km/h, zero pageerrors).

**Calibração se ainda estolar:** subir o fundo do casco em passos de 0,05 (off.y 0.15→0.20→0.25 com half.y 0.40→0.35→0.30, teto +0.15 de elevação) e/ou encolher a barriga em X para [−0.5, +1.3]. A barriga NUNCA sai. Se no teto ainda estolar, PARAR e reportar telemetria de contato (qual caixa toca).

- [ ] **Step 4: Regressão de veículos + lint**

Run: `node --test --test-concurrency=1 test/car-terrain-traversal.test.js test/car-settle.test.js test/car-remote-wheels.test.js test/car-models.test.js && npm run lint`
Expected: PASS — travessia (incl. caso-controle da árvore: o casco a +0.05 AINDA tem que colidir com tronco), assentamento/sono (inércia nova não pode reintroduzir jitter), rodas remotas, modelos. Flake → isolado 2-3×. Regressão consistente → parar e reportar.

- [ ] **Step 5: Commit**

```bash
git add js/car.js test/car-hillstart.test.js
git commit -m "fix(veiculos): collider do caminhao em 2 caixas — angulo de ataque real, fim do encalhe de barriga"
```

---

### Task 2: Gap 12 — medição A/B nesta máquina + gate

**Files:**
- Modify: `docs/plans/2026-07-17-gaps-8-13-mapa.md` (seção de resultados da medição)
- (Nenhum código de jogo — a instrumentação `?segs=` já existe, commit `a2f9437`.)

**Pré-condição:** Task 1 commitada e NENHUM teste/subagente rodando (máquina quieta).

- [ ] **Step 1: Servidor de medição** — `PORT=3250 node server.js` em background (a porta 3000 do dev fica intocada).
- [ ] **Step 2: Lado A** — abrir no Chrome REAL desta máquina `http://localhost:3250/?qa=1`, aguardar boot, rodar o snippet de p50/p95 do protocolo (mapa) 2×, anotar também `renderer.info.render.triangles` e `performance.memory.usedJSHeapSize`.
- [ ] **Step 3: Lado B** — mesma coisa em `http://localhost:3250/?qa=1&segs=440` (esperar o `console.warn` `[A/B] TERRAIN_SEGS=440`).
- [ ] **Step 4: Encerrar** o servidor 3250. Registrar os números no mapa, aplicar o GATE do protocolo: frame p95 do lado B ≤ 1,15× do lado A e tris/memória aceitáveis.
- [ ] **Step 5: Decisão registrada no mapa + backlog:**
  - **Gate PASSA** ⇒ abrir task de adoção (mudança única: `CFG.TERRAIN_SEGS` 220→440 em `js/config.js` + re-baseline dos testes de mundo + `npm test` completo). A adoção muda o layout do mundo pro mesmo seed — é UMA troca global, nunca opção por cliente.
  - **Gate FALHA** ⇒ item 12 fecha como "medido e recusado: 5 m fica" — registrar números e encerrar.
- [ ] **Step 6: Commit**

```bash
git add docs/plans/2026-07-17-gaps-8-13-mapa.md docs/2026-07-17-gaps-backlog.md
git commit -m "docs(terreno): A/B 5m x 2,5m medido em Chrome real — decisao pelo gate registrada"
```

---

### Task 3 (CONDICIONAL — só se o gate da Task 2 passar): adoção do 2,5 m

**Files:**
- Modify: `js/config.js:13` (`TERRAIN_SEGS: 440`)
- Modify: baselines que quebrarem (ex.: drawCalls da torre, superfícies de terreno) — cada re-baseline com justificativa no commit.

- [ ] **Step 1:** `TERRAIN_SEGS: 440` em js/config.js (comentário: célula 2,5 m, decisão A/B com números).
- [ ] **Step 2:** `npm test` completo. Falhas de baseline de MUNDO (layout mudou pro mesmo seed) são esperadas — re-baseline uma a uma, com o valor novo verificado à mão. Falha que NÃO é baseline = regressão: parar e reportar.
- [ ] **Step 3:** Commit `feat(terreno): grade canonica 2,5m adotada pelo A/B (fisica + visual + heightAt juntos)`.

## Self-Review

- Gap 8: mecanismo ataca a causa-raiz MEDIDA (contato do chassi), não hipóteses já descartadas; consumidores da forma verificados um a um; crista preservada por construção; calibração com teto e abort definido.
- Gap 12: instrumentação pronta, protocolo pronto, gate objetivo; adoção isolada em task condicional com re-baseline explícito.
- Nada aqui toca RNG de worldgen, rede, dano ou autoridade.
